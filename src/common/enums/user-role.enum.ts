/**
 * Rol global persistido en `users.role`.
 * Distinto del rol de empresa (`enterprise_roles`), que es un nombre libre.
 */
export enum UserRoleTypes {
  USER = 'user',
  ADMIN = 'administrator',
}
