/**
 * Tipo de proveedor persistido en `suppliers.type`.
 * A diferencia de clientes, el particular se guarda como `individual`.
 */
export enum SupplierType {
  COMPANY = 'company',
  INDIVIDUAL = 'individual',
}
